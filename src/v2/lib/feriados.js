// Feriados — feature futura.
// Será integrado com uma lista do ClickUp contendo os feriados do ano (nome + data).
// Por enquanto o cap é fixo em 40h.

export function getWeekCap(_year, _week) {
  return 40;
}
