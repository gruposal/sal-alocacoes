# Deploy via Vercel (conectado ao GitHub — auto-deploy a cada push em main).
# Este workflow GitHub Pages foi aposentado em 2026-05-18.
# Mantido no repo para histórico.

on:
  workflow_dispatch: # só roda se chamado manualmente

name: Deploy to GitHub Pages (aposentado)

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy via Vercel. Este workflow está desativado."
